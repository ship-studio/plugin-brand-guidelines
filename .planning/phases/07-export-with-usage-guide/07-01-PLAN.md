---
phase: 07-export-with-usage-guide
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/markdown.ts
  - src/markdown.test.ts
autonomous: true
requirements: [EX-01, EX-02]

must_haves:
  truths:
    - "Exported markdown contains a Usage Guide section with per-category subsections when usageSummaries exist"
    - "Usage Guide only includes subsections for categories that have accepted tokens"
    - "Export without usageSummaries (manual workflow) produces no Usage Guide section"
    - "Usage Guide appears after the existing Brand Guidelines sections (Colors, Fonts, Voice, etc.)"
  artifacts:
    - path: "src/markdown.ts"
      provides: "generateBrandMarkdown extended with Usage Guide section"
      exports: ["generateBrandMarkdown"]
    - path: "src/markdown.test.ts"
      provides: "Tests for Usage Guide generation"
      min_lines: 40
  key_links:
    - from: "src/markdown.ts"
      to: "BrandSettings.usageSummaries"
      via: "generateBrandMarkdown reads usageSummaries from settings param"
      pattern: "settings\\.usageSummaries"
---

<objective>
Extend generateBrandMarkdown to include a "Usage Guide" section when BrandSettings has usageSummaries.

Purpose: Complete the v1.1 Usage Guide feature by ensuring exported markdown includes actionable usage guidance per token category (EX-01), only for accepted tokens (EX-02), while gracefully omitting the section for manual-only workflows.

Output: Updated markdown.ts with Usage Guide generation, comprehensive test coverage in markdown.test.ts
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-ui-for-usage-summaries/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types.ts:
```typescript
export interface BrandSettings {
  colors: BrandColor[];
  fonts: BrandFont[];
  voiceNotes: string;
  assets: BrandAsset[];
  radii: BrandRadius[];
  spacing: BrandSpacing[];
  usageSummaries?: import('./analyzeTokens').UsageSummaries;
  targetFile: 'CLAUDE.md' | 'AGENTS.md';
  lastExportedHash: string;
}
```

From src/analyzeTokens.ts:
```typescript
export interface UsageSummaries {
  colors: string;
  fonts: string;
  radii: string;
  spacing: string;
}
```

From src/markdown.ts (current generateBrandMarkdown signature):
```typescript
export function generateBrandMarkdown(settings: BrandSettings): string
// Currently renders: ## Brand Guidelines with ### Colors, ### Fonts, ### Voice & Tone, ### Assets, ### Border Radii, ### Spacing
// Returns empty string if no valid tokens
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD generateBrandMarkdown Usage Guide section</name>
  <files>src/markdown.test.ts, src/markdown.ts</files>
  <behavior>
    - Test 1: generateBrandMarkdown with usageSummaries produces a "### Usage Guide" section containing per-category subsections (#### Colors, #### Fonts, #### Border Radii, #### Spacing)
    - Test 2: generateBrandMarkdown without usageSummaries (undefined) produces NO Usage Guide section — existing output unchanged
    - Test 3: generateBrandMarkdown with empty-string usageSummaries ({ colors: '', fonts: '', radii: '', spacing: '' }) produces NO Usage Guide section
    - Test 4: generateBrandMarkdown with partial usageSummaries (only colors has content, fonts/radii/spacing are empty) produces Usage Guide with only the Colors subsection
    - Test 5: Usage Guide only includes subsections for categories that have valid tokens in settings (e.g., if settings.colors is empty but usageSummaries.colors has text, skip that subsection)
    - Test 6: Usage Guide section appears after all existing Brand Guidelines subsections
  </behavior>
  <action>
RED phase: Create src/markdown.test.ts with tests for generateBrandMarkdown. Import generateBrandMarkdown from './markdown'. Build a helper function that creates a minimal valid BrandSettings object for test use. Write all 6 tests. Run tests — they MUST fail (Usage Guide not implemented yet). Commit: test(07-01): add failing tests for Usage Guide export

GREEN phase: Modify generateBrandMarkdown in src/markdown.ts to add a Usage Guide section at the end. Logic:
1. After building existing sections array, check if settings.usageSummaries exists and is not all-empty
2. Build a usageGuide subsections array. For each category key ('colors', 'fonts', 'radii', 'spacing'), include a subsection ONLY if:
   a. The usage summary string for that key is non-empty (after trim), AND
   b. The corresponding token array in settings has at least one valid entry (using same validation as existing sections — colors: name && hex, fonts: role && value, radii: label && value, spacing: label && value)
3. Map category keys to display names: colors -> "Colors", fonts -> "Fonts", radii -> "Border Radii", spacing -> "Spacing"
4. If any subsections exist, push a "### Usage Guide\n\n" + subsections joined by "\n\n" to the sections array
5. Each subsection format: "#### {DisplayName}\n\n{summaryText}"

Run tests — they MUST pass. Commit: feat(07-01): add Usage Guide section to generateBrandMarkdown

REFACTOR phase (if needed): Clean up, ensure no duplication. Run tests — MUST still pass.
  </action>
  <verify>
    <automated>npx vitest run src/markdown.test.ts</automated>
  </verify>
  <done>generateBrandMarkdown produces a Usage Guide section with per-category subsections when usageSummaries exist. No Usage Guide when summaries are absent or all empty. Only categories with both valid tokens and non-empty summaries get subsections.</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end export includes Usage Guide</name>
  <files>src/markdown.test.ts</files>
  <action>
Add one integration-style test to src/markdown.test.ts that exercises the full export pipeline path:
1. Create a BrandSettings with colors, fonts, radii, spacing, voiceNotes, assets, AND usageSummaries
2. Call generateBrandMarkdown
3. Assert the output contains "## Brand Guidelines" at the top
4. Assert it contains existing subsections (### Colors, ### Fonts, ### Voice & Tone, etc.)
5. Assert it contains "### Usage Guide" after all token sections
6. Assert it contains "#### Colors" and "#### Fonts" subsections within Usage Guide
7. Call hasBrandData with same settings — assert returns true
8. Call djb2Hash on the output — assert it returns a non-empty string (hash changes when usage guide changes)
9. Call buildFileContent with null existing content — assert result contains the usage guide wrapped in markers

Also add one test: buildFileContent with existing content and markers replaces section INCLUDING usage guide.

Run all tests. Commit: test(07-01): add integration test for full export with Usage Guide
  </action>
  <verify>
    <automated>npx vitest run src/markdown.test.ts</automated>
  </verify>
  <done>Full export pipeline verified: generateBrandMarkdown, hasBrandData, djb2Hash, and buildFileContent all work correctly with usage summaries present. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/markdown.test.ts` — all tests pass
2. `npx vitest run` — full test suite still passes (no regressions)
3. `npm run build` — builds successfully
</verification>

<success_criteria>
- generateBrandMarkdown produces Usage Guide section when usageSummaries exist on BrandSettings (EX-01)
- Usage Guide only includes subsections for categories with valid accepted tokens (EX-02)
- No Usage Guide section when usageSummaries is undefined or all empty strings (graceful degradation)
- All existing tests continue to pass (no regressions)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-with-usage-guide/07-01-SUMMARY.md`
</output>
